# Real file: deploy/service.yaml (untracked, see .git/info/exclude).
apiVersion: v1
kind: Service
metadata:
  name: <PREFIX>urs-web
  namespace: urs
spec:
  type: ClusterIP
  selector:
    app: <PREFIX>urs-web
  ports:
    - name: http
      port: 80
      targetPort: 80
